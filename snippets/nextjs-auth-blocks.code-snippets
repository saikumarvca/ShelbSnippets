{
  "Shelb Next Login — Centered (snxlogin01)": {
    "prefix": "snxlogin01",
    "description": "Next.js App Router centered login page (use client + useRouter + fetch).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:LoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "      if (!res.ok) throw new Error(\"Invalid email or password.\");",
      "      router.push(\"${3:/dashboard}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-2xl font-semibold text-gray-900 text-center\">Sign in to ${4:Shelb}</h1>",
      "        {error && <div role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</div>}",
      "        <div className=\"space-y-1\"><label htmlFor=\"email\" className=\"block text-sm font-medium text-gray-700\">Email</label><input id=\"email\" type=\"email\" autoComplete=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label htmlFor=\"password\" className=\"block text-sm font-medium text-gray-700\">Password</label><input id=\"password\" type=\"password\" autoComplete=\"current-password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Split Screen (snxlogin02)": {
    "prefix": "snxlogin02",
    "description": "Next.js split-screen login page with brand panel and form panel.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:SplitLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "      if (!res.ok) throw new Error(\"Invalid credentials.\");",
      "      router.push(\"${3:/dashboard}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen grid lg:grid-cols-2}\">",
      "      <aside className=\"hidden lg:flex flex-col justify-between bg-gradient-to-br from-blue-700 to-indigo-800 text-white p-10\"><span className=\"text-2xl font-bold\">${4:Shelb}</span><p className=\"text-blue-100\">The operating system for modern teams.</p><span className=\"text-xs text-blue-200\">© ${4:Shelb} Technologies</span></aside>",
      "      <section className=\"flex items-center justify-center p-6\">",
      "        <form onSubmit={onSubmit} className=\"w-full max-w-md space-y-5\">",
      "          <h1 className=\"text-2xl font-semibold text-gray-900\">Welcome back</h1>",
      "          {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-gray-900 hover:bg-gray-800 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "          $0",
      "        </form>",
      "      </section>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Admin (snxlogin03)": {
    "prefix": "snxlogin03",
    "description": "Next.js admin login page with admin-only wording and secure portal copy.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:AdminLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/admin/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "      if (!res.ok) throw new Error(\"Access denied.\");",
      "      router.push(\"${3:/admin}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-900 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-2xl p-8 space-y-5 border-t-4 border-red-600\">",
      "        <header><p className=\"text-xs uppercase tracking-widest text-red-700\">Restricted</p><h1 className=\"text-xl font-semibold text-gray-900\">${4:Shelb} admin portal</h1></header>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Admin email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-red-600 hover:bg-red-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Authenticating…\" : \"${6:Access admin}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — SaaS (snxlogin04)": {
    "prefix": "snxlogin04",
    "description": "Next.js SaaS workspace login page with sign-up footer link.",
    "body": [
      "\"use client\";",
      "import Link from \"next/link\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:SaaSLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "      if (!res.ok) throw new Error(\"Invalid workspace credentials.\");",
      "      router.push(\"${3:/app}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex flex-col items-center justify-center bg-gray-50 px-4}\">",
      "      <Link href=\"/\" className=\"mb-6 flex items-center gap-2\"><span className=\"h-8 w-8 rounded-md bg-blue-600 text-white grid place-items-center font-bold\">S</span><span className=\"font-semibold text-gray-900\">${4:Shelb}</span></Link>",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <header><h1 className=\"text-xl font-semibold text-gray-900\">Sign in to your workspace</h1><p className=\"text-sm text-gray-500\">Continue to ${4:Shelb}.</p></header>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Work email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "        <p className=\"text-xs text-gray-500 text-center\">No account? <Link href=\"/signup\" className=\"text-blue-700 hover:underline\">Start free trial</Link></p>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — ERP with Company Code (snxlogin05)": {
    "prefix": "snxlogin05",
    "description": "Next.js ERP login with company code, user ID and password.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:ERPLoginPage}() {",
      "  const router = useRouter();",
      "  const [companyCode, setCompanyCode] = useState(\"\");",
      "  const [userId, setUserId] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/erp-login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ companyCode, userId, password }) });",
      "      if (!res.ok) throw new Error(\"Invalid company code or credentials.\");",
      "      router.push(\"${3:/erp}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-slate-100 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <header><p className=\"text-xs uppercase tracking-widest text-blue-700\">${4:Shelb} ERP</p><h1 className=\"text-xl font-semibold text-gray-900\">Enterprise sign-in</h1></header>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Company code</label><input value={companyCode} onChange={(e) => setCompanyCode(e.target.value.toUpperCase())} required className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm uppercase tracking-widest\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">User ID</label><input value={userId} onChange={(e) => setUserId(e.target.value)} required className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" value={password} onChange={(e) => setPassword(e.target.value)} required className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-700 hover:bg-blue-800 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Verifying…\" : \"${6:Sign in to ERP}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — OTP (snxlogin06)": {
    "prefix": "snxlogin06",
    "description": "Next.js OTP login with phone-step then 6-digit code-step.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:OtpLoginPage}() {",
      "  const router = useRouter();",
      "  const [step, setStep] = useState<\"phone\" | \"otp\">(\"phone\");",
      "  const [phone, setPhone] = useState(\"\");",
      "  const [otp, setOtp] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function request(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); setError(null); try { const res = await fetch(\"${2:/api/auth/otp/request}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ phone }) }); if (!res.ok) throw new Error(\"Could not send code.\"); setStep(\"otp\"); } catch (err) { setError((err as Error).message); } finally { setLoading(false); } }",
      "  async function verify(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); setError(null); try { const res = await fetch(\"${2:/api/auth/otp/verify}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ phone, otp }) }); if (!res.ok) throw new Error(\"Invalid code.\"); router.push(\"${3:/dashboard}\"); } catch (err) { setError((err as Error).message); } finally { setLoading(false); } }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <div className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        {step === \"phone\" ? (",
      "          <form onSubmit={request} className=\"space-y-4\"><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Phone number</label><input type=\"tel\" required value={phone} onChange={(e) => setPhone(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div><button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Sending…\" : \"${6:Send code}\"}</button></form>",
      "        ) : (",
      "          <form onSubmit={verify} className=\"space-y-4\"><p className=\"text-sm text-gray-500\">Enter the 6-digit code sent to {phone}.</p><input value={otp} onChange={(e) => setOtp(e.target.value)} required maxLength={6} inputMode=\"numeric\" className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-center text-lg tracking-[0.6em]\" /><button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Verifying…\" : \"Verify code\"}</button></form>",
      "        )}",
      "        $0",
      "      </div>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Magic Link (snxlogin07)": {
    "prefix": "snxlogin07",
    "description": "Next.js magic-link login (email-only, awaiting email confirmation).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "",
      "export default function ${1:MagicLinkLoginPage}() {",
      "  const [email, setEmail] = useState(\"\");",
      "  const [sent, setSent] = useState(false);",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/magic-link}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, redirectTo: \"${3:/dashboard}\" }) });",
      "      if (!res.ok) throw new Error(\"Could not send magic link.\");",
      "      setSent(true);",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <div className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        {sent ? (",
      "          <p role=\"status\" className=\"rounded-md bg-emerald-50 text-emerald-700 px-3 py-2 text-sm\">Magic link sent. Check your inbox.</p>",
      "        ) : (",
      "          <form onSubmit={onSubmit} className=\"space-y-4\">",
      "            {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "            <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "            <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Sending…\" : \"${6:Email me a magic link}\"}</button>",
      "          </form>",
      "        )}",
      "        $0",
      "      </div>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Social Providers (snxlogin08)": {
    "prefix": "snxlogin08",
    "description": "Next.js login page with social provider buttons (Google / GitHub / Microsoft).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:SocialLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true);",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "    setLoading(false);",
      "  }",
      "  function social(provider: string) { window.location.href = \"/api/auth/oauth/\" + provider; }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900 text-center\">Sign in to ${4:Shelb}</h1>",
      "        <div className=\"grid grid-cols-1 gap-2\">",
      "          <button type=\"button\" onClick={() => social(\"google\")} className=\"rounded-md border border-gray-300 px-4 py-2 text-sm hover:bg-gray-50\">Continue with Google</button>",
      "          <button type=\"button\" onClick={() => social(\"github\")} className=\"rounded-md border border-gray-300 px-4 py-2 text-sm hover:bg-gray-50\">Continue with GitHub</button>",
      "          <button type=\"button\" onClick={() => social(\"microsoft\")} className=\"rounded-md border border-gray-300 px-4 py-2 text-sm hover:bg-gray-50\">Continue with Microsoft</button>",
      "        </div>",
      "        <div className=\"flex items-center gap-3 text-xs text-gray-400\"><div className=\"h-px flex-1 bg-gray-200\" /><span>or</span><div className=\"h-px flex-1 bg-gray-200\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Dark (snxlogin09)": {
    "prefix": "snxlogin09",
    "description": "Next.js dark-themed login page with developer/admin feel.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:DarkLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "      if (!res.ok) throw new Error(\"Invalid credentials.\");",
      "      router.push(\"${3:/dashboard}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-zinc-950 text-zinc-100 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-zinc-900 border border-zinc-800 rounded-2xl shadow-2xl p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold\">Sign in to ${4:Shelb}</h1>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-950/50 text-red-300 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-zinc-300\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md bg-zinc-950 border border-zinc-800 text-zinc-100 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-zinc-300\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md bg-zinc-950 border border-zinc-800 text-zinc-100 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-emerald-500 hover:bg-emerald-400 text-zinc-950 px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Minimal (snxlogin10)": {
    "prefix": "snxlogin10",
    "description": "Next.js minimal centered login page with no extras.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:MinimalLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen grid place-items-center bg-white px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-sm space-y-4\">",
      "        <h1 className=\"text-lg font-semibold text-center\">${4:Shelb}</h1>",
      "        <input type=\"email\" placeholder=\"Email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" />",
      "        <input type=\"password\" placeholder=\"Password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" />",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-gray-900 hover:bg-gray-800 text-white px-4 py-2 text-sm\">${6:Continue}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Remember Me (snxlogin11)": {
    "prefix": "snxlogin11",
    "description": "Next.js login with persistent session toggle (remember me).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:RememberLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [remember, setRemember] = useState(true);",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password, remember }) });",
      "      if (!res.ok) throw new Error(\"Invalid credentials.\");",
      "      router.push(\"${3:/dashboard}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <label className=\"inline-flex items-center gap-2 text-sm text-gray-700\"><input type=\"checkbox\" checked={remember} onChange={(e) => setRemember(e.target.checked)} />Keep me signed in</label>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Role Selection (snxlogin12)": {
    "prefix": "snxlogin12",
    "description": "Next.js login with role selection (Admin / Manager / Employee).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:RoleLoginPage}() {",
      "  const router = useRouter();",
      "  const [role, setRole] = useState(\"Employee\");",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password, role }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        <div className=\"grid grid-cols-3 gap-2\">{[\"Admin\", \"Manager\", \"Employee\"].map((r) => (<button key={r} type=\"button\" onClick={() => setRole(r)} className={\"rounded-md border px-3 py-2 text-sm \" + (role === r ? \"border-blue-600 bg-blue-50 text-blue-700\" : \"border-gray-200 text-gray-700 hover:bg-gray-50\")}>{r}</button>))}</div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Sign in as} {role}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Tenant (snxlogin13)": {
    "prefix": "snxlogin13",
    "description": "Next.js tenant login with workspace subdomain field.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:TenantLoginPage}() {",
      "  const router = useRouter();",
      "  const [tenant, setTenant] = useState(\"\");",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ tenant, email, password }) });",
      "    router.push(\"${3:/}\" + tenant + \"/dashboard\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Workspace</label><div className=\"flex rounded-md border border-gray-300 overflow-hidden\"><input value={tenant} onChange={(e) => setTenant(e.target.value)} required className=\"flex-1 px-3 py-2 text-sm\" placeholder=\"acme\" /><span className=\"px-3 py-2 text-sm bg-gray-50 text-gray-500\">.${4:shelb}.app</span></div></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Continue}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Validation (snxlogin14)": {
    "prefix": "snxlogin14",
    "description": "Next.js login page with inline client-side validation messages.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:ValidatedLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [touched, setTouched] = useState({ email: false, password: false });",
      "  const emailError = !email ? \"Email is required\" : !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email) ? \"Invalid email\" : \"\";",
      "  const passwordError = !password ? \"Password is required\" : password.length < 8 ? \"Min 8 characters\" : \"\";",
      "  const isValid = !emailError && !passwordError;",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setTouched({ email: true, password: true }); if (!isValid) return;",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form noValidate onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" value={email} onChange={(e) => setEmail(e.target.value)} onBlur={() => setTouched((t) => ({ ...t, email: true }))} aria-invalid={touched.email && !!emailError} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm aria-[invalid=true]:border-red-500\" />{touched.email && emailError && <p className=\"text-xs text-red-600\">{emailError}</p>}</div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" value={password} onChange={(e) => setPassword(e.target.value)} onBlur={() => setTouched((t) => ({ ...t, password: true }))} aria-invalid={touched.password && !!passwordError} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm aria-[invalid=true]:border-red-500\" />{touched.password && passwordError && <p className=\"text-xs text-red-600\">{passwordError}</p>}</div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Sign in}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Loading State (snxlogin15)": {
    "prefix": "snxlogin15",
    "description": "Next.js login with explicit loading spinner and aria-busy state.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:LoadingLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [loading, setLoading] = useState(false);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true);",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "    setLoading(false);",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} aria-busy={loading} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"inline-flex w-full items-center justify-center gap-2 rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading && (<svg className=\"animate-spin h-4 w-4\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><circle cx=\"12\" cy=\"12\" r=\"10\" stroke=\"currentColor\" strokeWidth=\"4\" fill=\"none\" className=\"opacity-25\" /><path d=\"M4 12a8 8 0 018-8\" stroke=\"currentColor\" strokeWidth=\"4\" fill=\"none\" className=\"opacity-75\" /></svg>)}{loading ? \"Signing in…\" : \"${6:Sign in}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Forgot Password (snxlogin16)": {
    "prefix": "snxlogin16",
    "description": "Next.js login with forgot-password link and sign-up secondary link.",
    "body": [
      "\"use client\";",
      "import Link from \"next/link\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:ForgotPasswordLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><div className=\"flex items-center justify-between\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><Link href=\"/auth/forgot\" className=\"text-xs text-blue-700 hover:underline\">Forgot password?</Link></div><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Sign in}</button>",
      "        <p className=\"text-xs text-gray-500 text-center\">New here? <Link href=\"/signup\" className=\"text-blue-700 hover:underline\">Create an account</Link></p>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Mobile First (snxlogin17)": {
    "prefix": "snxlogin17",
    "description": "Next.js mobile-first full-bleed login page with thumb-friendly inputs.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:MobileLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-[100svh] flex flex-col bg-white px-5 pb-8 pt-12}\">",
      "      <header className=\"text-center mb-8\"><h1 className=\"text-3xl font-bold\">${4:Shelb}</h1><p className=\"text-sm text-gray-500 mt-1\">Sign in to continue</p></header>",
      "      <form onSubmit={onSubmit} className=\"flex-1 flex flex-col gap-4\">",
      "        <input type=\"email\" placeholder=\"Email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-xl border border-gray-200 px-4 py-4 text-base\" />",
      "        <input type=\"password\" placeholder=\"Password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-xl border border-gray-200 px-4 py-4 text-base\" />",
      "        <button type=\"submit\" className=\"mt-auto w-full rounded-xl bg-blue-600 hover:bg-blue-700 text-white px-4 py-4 text-base font-medium\">${6:Sign in}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Enterprise Branded (snxlogin18)": {
    "prefix": "snxlogin18",
    "description": "Next.js enterprise-branded login with logo, SSO callout and policy footer.",
    "body": [
      "\"use client\";",
      "import Link from \"next/link\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:BrandedLoginPage}() {",
      "  const router = useRouter();",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex flex-col bg-gradient-to-b from-slate-50 to-white px-4}\">",
      "      <div className=\"flex-1 flex items-center justify-center\">",
      "        <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 p-8 space-y-5\">",
      "          <div className=\"flex items-center justify-between\"><div className=\"flex items-center gap-2\"><span className=\"h-9 w-9 rounded-lg bg-blue-700 text-white grid place-items-center font-bold\">S</span><span className=\"font-semibold text-gray-900\">${4:Shelb}</span></div><Link href=\"/sso\" className=\"text-xs text-blue-700 hover:underline\">Use SSO</Link></div>",
      "          <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to your account</h1>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Work email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <button type=\"submit\" className=\"w-full rounded-md bg-blue-700 hover:bg-blue-800 text-white px-4 py-2 font-medium\">${6:Sign in}</button>",
      "          $0",
      "        </form>",
      "      </div>",
      "      <footer className=\"py-6 text-center text-xs text-gray-400\">© ${4:Shelb} · <Link href=\"/privacy\" className=\"hover:underline\">Privacy</Link> · <Link href=\"/terms\" className=\"hover:underline\">Terms</Link></footer>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Two Factor (snxlogin19)": {
    "prefix": "snxlogin19",
    "description": "Next.js login page with 2FA placeholder (credentials → TOTP code).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:TwoFactorLoginPage}() {",
      "  const router = useRouter();",
      "  const [step, setStep] = useState<\"creds\" | \"2fa\">(\"creds\");",
      "  const [email, setEmail] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  const [code, setCode] = useState(\"\");",
      "  async function creds(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) }); if (res.ok) setStep(\"2fa\"); }",
      "  async function verify(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const res = await fetch(\"/api/auth/2fa\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ code }) }); if (res.ok) router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <div className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Sign in to ${4:Shelb}</h1>",
      "        {step === \"creds\" ? (",
      "          <form onSubmit={creds} className=\"space-y-4\"><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div><button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">Continue</button></form>",
      "        ) : (",
      "          <form onSubmit={verify} className=\"space-y-4\"><p className=\"text-sm text-gray-500\">Enter the 6-digit code from your authenticator app.</p><input value={code} onChange={(e) => setCode(e.target.value)} required maxLength={6} inputMode=\"numeric\" className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-center text-lg tracking-[0.6em]\" /><button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Verify}</button></form>",
      "        )}",
      "        $0",
      "      </div>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Login — Session Expired (snxlogin20)": {
    "prefix": "snxlogin20",
    "description": "Next.js re-authentication page shown when the session has expired.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter, useSearchParams } from \"next/navigation\";",
      "",
      "export default function ${1:SessionExpiredPage}() {",
      "  const router = useRouter();",
      "  const params = useSearchParams();",
      "  const next = params.get(\"next\") ?? \"${3:/dashboard}\";",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/reauth}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ password }) });",
      "    router.push(next);",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5 border-t-4 border-amber-500\">",
      "        <header><p className=\"text-xs uppercase tracking-widest text-amber-700\">Session expired</p><h1 className=\"text-xl font-semibold text-gray-900\">Confirm it is you</h1><p className=\"text-sm text-gray-500 mt-1\">Please re-enter your password to continue on ${4:Shelb}.</p></header>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 font-medium\">${6:Continue}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Basic (snxreg01)": {
    "prefix": "snxreg01",
    "description": "Next.js basic register page with name, email, password.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:RegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) });",
      "      if (!res.ok) throw new Error(\"Could not create account.\");",
      "      router.push(\"${3:/dashboard}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Full name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading ? \"Creating account…\" : \"${6:Create account}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — SaaS Signup (snxreg02)": {
    "prefix": "snxreg02",
    "description": "Next.js SaaS signup page with workspace + admin user fields.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:SaaSSignupPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ workspace: \"\", name: \"\", email: \"\", password: \"\" });",
      "  function set<K extends keyof typeof data>(k: K, v: string) { setData((d) => ({ ...d, [k]: v })); }",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/signup}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) });",
      "    router.push(\"${3:/onboarding}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <header><h1 className=\"text-xl font-semibold text-gray-900\">Start your ${4:Shelb} workspace</h1><p className=\"text-sm text-gray-500\">14-day free trial · no credit card required.</p></header>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Workspace</label><div className=\"flex rounded-md border border-gray-300 overflow-hidden\"><input value={data.workspace} onChange={(e) => set(\"workspace\", e.target.value)} required className=\"flex-1 px-3 py-2 text-sm\" placeholder=\"acme\" /><span className=\"px-3 py-2 text-sm bg-gray-50 text-gray-500\">.${4:shelb}.app</span></div></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Your name</label><input required value={data.name} onChange={(e) => set(\"name\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Work email</label><input type=\"email\" required value={data.email} onChange={(e) => set(\"email\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => set(\"password\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Start free trial}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — ERP Company (snxreg03)": {
    "prefix": "snxreg03",
    "description": "Next.js ERP company onboarding registration with GSTIN and contact.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:ERPCompanyRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ companyName: \"\", gstin: \"\", contactName: \"\", email: \"\", password: \"\" });",
      "  function set<K extends keyof typeof data>(k: K, v: string) { setData((d) => ({ ...d, [k]: v })); }",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/erp/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/erp/onboarding}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-slate-100 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-lg bg-white rounded-2xl shadow-lg p-8 space-y-4\">",
      "        <header><p className=\"text-xs uppercase tracking-widest text-blue-700\">${4:Shelb} ERP</p><h1 className=\"text-xl font-semibold text-gray-900\">Register your company</h1></header>",
      "        <div className=\"grid sm:grid-cols-2 gap-4\">",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Company name</label><input required value={data.companyName} onChange={(e) => set(\"companyName\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">GSTIN</label><input maxLength={15} required value={data.gstin} onChange={(e) => set(\"gstin\", e.target.value.toUpperCase())} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm uppercase tracking-widest\" /></div>",
      "        </div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Contact person</label><input required value={data.contactName} onChange={(e) => set(\"contactName\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"grid sm:grid-cols-2 gap-4\">",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Business email</label><input type=\"email\" required value={data.email} onChange={(e) => set(\"email\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => set(\"password\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        </div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-700 hover:bg-blue-800 text-white px-4 py-2 font-medium\">${6:Register company}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Multi-Step (snxreg04)": {
    "prefix": "snxreg04",
    "description": "Next.js multi-step register page (account → company → done).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:MultiStepRegisterPage}() {",
      "  const router = useRouter();",
      "  const [step, setStep] = useState(1);",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\", company: \"\", size: \"1-10\" });",
      "  function set<K extends keyof typeof data>(k: K, v: string) { setData((d) => ({ ...d, [k]: v })); }",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    if (step < 2) return setStep(step + 1);",
      "    await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <div className=\"flex gap-2\">{[1, 2].map((n) => <div key={n} className={\"flex-1 h-1 rounded \" + (n <= step ? \"bg-blue-600\" : \"bg-gray-200\")} />)}</div>",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        {step === 1 && (<><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Full name</label><input required value={data.name} onChange={(e) => set(\"name\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => set(\"email\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => set(\"password\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div></>)}",
      "        {step === 2 && (<><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Company name</label><input required value={data.company} onChange={(e) => set(\"company\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div><div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Team size</label><select value={data.size} onChange={(e) => set(\"size\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm bg-white\"><option>1-10</option><option>11-50</option><option>51-200</option><option>200+</option></select></div></>)}",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">{step < 2 ? \"Continue\" : \"${6:Finish}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Social (snxreg05)": {
    "prefix": "snxreg05",
    "description": "Next.js register page with social providers + email/password fallback.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:SocialRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  function social(p: string) { window.location.href = \"/api/auth/oauth/\" + p; }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-4\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900 text-center\">Join ${4:Shelb}</h1>",
      "        <button type=\"button\" onClick={() => social(\"google\")} className=\"w-full rounded-md border border-gray-300 px-4 py-2 text-sm hover:bg-gray-50\">Sign up with Google</button>",
      "        <button type=\"button\" onClick={() => social(\"github\")} className=\"w-full rounded-md border border-gray-300 px-4 py-2 text-sm hover:bg-gray-50\">Sign up with GitHub</button>",
      "        <div className=\"flex items-center gap-3 text-xs text-gray-400\"><div className=\"h-px flex-1 bg-gray-200\" /><span>or</span><div className=\"h-px flex-1 bg-gray-200\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — OTP (snxreg06)": {
    "prefix": "snxreg06",
    "description": "Next.js phone-OTP register with phone → code → profile steps.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:OtpRegisterPage}() {",
      "  const router = useRouter();",
      "  const [step, setStep] = useState<\"phone\" | \"otp\" | \"profile\">(\"phone\");",
      "  const [phone, setPhone] = useState(\"\");",
      "  const [otp, setOtp] = useState(\"\");",
      "  const [name, setName] = useState(\"\");",
      "  async function send(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/otp/request}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ phone }) }); setStep(\"otp\"); }",
      "  async function verify(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const res = await fetch(\"/api/auth/otp/verify\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ phone, otp }) }); if (res.ok) setStep(\"profile\"); }",
      "  async function complete(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"/api/auth/register\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ phone, name }) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <div className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Join ${4:Shelb}</h1>",
      "        {step === \"phone\" && <form onSubmit={send} className=\"space-y-4\"><input type=\"tel\" placeholder=\"Phone number\" required value={phone} onChange={(e) => setPhone(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /><button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">Send code</button></form>}",
      "        {step === \"otp\" && <form onSubmit={verify} className=\"space-y-4\"><input value={otp} onChange={(e) => setOtp(e.target.value)} maxLength={6} inputMode=\"numeric\" placeholder=\"6-digit code\" className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-center tracking-[0.6em]\" /><button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">Verify</button></form>}",
      "        {step === \"profile\" && <form onSubmit={complete} className=\"space-y-4\"><input placeholder=\"Your name\" required value={name} onChange={(e) => setName(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /><button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Finish}</button></form>}",
      "        $0",
      "      </div>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Magic Link (snxreg07)": {
    "prefix": "snxreg07",
    "description": "Next.js magic-link registration (email-only).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "",
      "export default function ${1:MagicRegisterPage}() {",
      "  const [email, setEmail] = useState(\"\");",
      "  const [sent, setSent] = useState(false);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/magic-link}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, signup: true, redirectTo: \"${3:/dashboard}\" }) });",
      "    setSent(true);",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <div className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        {sent ? (",
      "          <p role=\"status\" className=\"rounded-md bg-emerald-50 text-emerald-700 px-3 py-2 text-sm\">Check your inbox for a sign-up link.</p>",
      "        ) : (",
      "          <form onSubmit={onSubmit} className=\"space-y-4\">",
      "            <input type=\"email\" placeholder=\"you@company.com\" required value={email} onChange={(e) => setEmail(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" />",
      "            <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Send sign-up link}</button>",
      "          </form>",
      "        )}",
      "        $0",
      "      </div>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Invite Only (snxreg08)": {
    "prefix": "snxreg08",
    "description": "Next.js invite-only registration accepting an invite token from the URL.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter, useSearchParams } from \"next/navigation\";",
      "",
      "export default function ${1:InviteRegisterPage}() {",
      "  const router = useRouter();",
      "  const token = useSearchParams().get(\"token\") ?? \"\";",
      "  const [name, setName] = useState(\"\");",
      "  const [password, setPassword] = useState(\"\");",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault();",
      "    await fetch(\"${2:/api/auth/invite/accept}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ token, name, password }) });",
      "    router.push(\"${3:/dashboard}\");",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <header><p className=\"text-xs uppercase tracking-widest text-blue-700\">Invitation</p><h1 className=\"text-xl font-semibold text-gray-900\">Join ${4:Shelb}</h1>{!token && <p className=\"text-sm text-red-600 mt-1\">Invalid or missing invite token.</p>}</header>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Full name</label><input required value={name} onChange={(e) => setName(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={password} onChange={(e) => setPassword(e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={!token} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">${6:Accept invite}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Dark (snxreg09)": {
    "prefix": "snxreg09",
    "description": "Next.js dark-themed register page.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:DarkRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-zinc-950 text-zinc-100 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-zinc-900 border border-zinc-800 rounded-2xl shadow-2xl p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold\">Create your ${4:Shelb} account</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-zinc-300\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md bg-zinc-950 border border-zinc-800 text-zinc-100 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-zinc-300\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md bg-zinc-950 border border-zinc-800 text-zinc-100 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-zinc-300\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md bg-zinc-950 border border-zinc-800 text-zinc-100 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-emerald-500 hover:bg-emerald-400 text-zinc-950 px-4 py-2 font-medium\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Minimal (snxreg10)": {
    "prefix": "snxreg10",
    "description": "Next.js minimal register page.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:MinimalRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen grid place-items-center bg-white px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-sm space-y-4\">",
      "        <h1 className=\"text-lg font-semibold text-center\">${4:Shelb}</h1>",
      "        <input type=\"email\" placeholder=\"Email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" />",
      "        <input type=\"password\" placeholder=\"Password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" />",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-gray-900 hover:bg-gray-800 text-white px-4 py-2 text-sm\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Terms Checkbox (snxreg11)": {
    "prefix": "snxreg11",
    "description": "Next.js register requiring terms-of-service checkbox.",
    "body": [
      "\"use client\";",
      "import Link from \"next/link\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:TermsRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  const [accept, setAccept] = useState(false);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); if (!accept) return; await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <label className=\"inline-flex items-start gap-2 text-sm text-gray-700\"><input type=\"checkbox\" checked={accept} onChange={(e) => setAccept(e.target.checked)} className=\"mt-1\" /><span>I agree to the <Link href=\"/terms\" className=\"text-blue-700 hover:underline\">Terms</Link> and <Link href=\"/privacy\" className=\"text-blue-700 hover:underline\">Privacy</Link>.</span></label>",
      "        <button type=\"submit\" disabled={!accept} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Password Strength (snxreg12)": {
    "prefix": "snxreg12",
    "description": "Next.js register with a 4-segment password strength meter.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "function strength(pw: string) { let s = 0; if (pw.length >= 8) s++; if (/[A-Z]/.test(pw)) s++; if (/[0-9]/.test(pw)) s++; if (/[^A-Za-z0-9]/.test(pw)) s++; return s; }",
      "",
      "export default function ${1:StrengthRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  const s = strength(data.password);",
      "  const colors = [\"bg-gray-200\", \"bg-red-500\", \"bg-amber-500\", \"bg-blue-500\", \"bg-emerald-500\"];",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); if (s < 3) return; await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /><div className=\"mt-2 flex gap-1\">{[1,2,3,4].map((i) => <div key={i} className={\"h-1 flex-1 rounded \" + (i <= s ? colors[s] : \"bg-gray-200\")} />)}</div><p className=\"text-xs text-gray-500\">Use 8+ chars with upper-case, digits and a symbol.</p></div>",
      "        <button type=\"submit\" disabled={s < 3} className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — With Company Code (snxreg13)": {
    "prefix": "snxreg13",
    "description": "Next.js register with company access code (employee onboarding).",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:CompanyCodeRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ code: \"\", name: \"\", email: \"\", password: \"\" });",
      "  function set<K extends keyof typeof data>(k: K, v: string) { setData((d) => ({ ...d, [k]: v })); }",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-4\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Join your team on ${4:Shelb}</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Company access code</label><input required value={data.code} onChange={(e) => set(\"code\", e.target.value.toUpperCase())} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm uppercase tracking-widest\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => set(\"name\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Work email</label><input type=\"email\" required value={data.email} onChange={(e) => set(\"email\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => set(\"password\", e.target.value)} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Join workspace}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Role Selection (snxreg14)": {
    "prefix": "snxreg14",
    "description": "Next.js register page with role selection chips.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:RoleRegisterPage}() {",
      "  const router = useRouter();",
      "  const [role, setRole] = useState(\"Employee\");",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ ...data, role }) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-4\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Join ${4:Shelb}</h1>",
      "        <p className=\"text-sm text-gray-500\">Pick the role that best describes you.</p>",
      "        <div className=\"grid grid-cols-3 gap-2\">{[\"Admin\", \"Manager\", \"Employee\"].map((r) => (<button key={r} type=\"button\" onClick={() => setRole(r)} className={\"rounded-md border px-3 py-2 text-sm \" + (role === r ? \"border-blue-600 bg-blue-50 text-blue-700\" : \"border-gray-200 text-gray-700 hover:bg-gray-50\")}>{r}</button>))}</div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Create} {role} account</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Tenant Subdomain (snxreg15)": {
    "prefix": "snxreg15",
    "description": "Next.js multi-tenant signup capturing subdomain at registration.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:TenantRegisterPage}() {",
      "  const router = useRouter();",
      "  const [tenant, setTenant] = useState(\"\");",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/tenants}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ tenant, ...data }) }); router.push(\"${3:/}\" + tenant + \"/dashboard\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-4\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} workspace</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Workspace</label><div className=\"flex rounded-md border border-gray-300 overflow-hidden\"><input value={tenant} onChange={(e) => setTenant(e.target.value)} required className=\"flex-1 px-3 py-2 text-sm\" placeholder=\"acme\" /><span className=\"px-3 py-2 text-sm bg-gray-50 text-gray-500\">.${4:shelb}.app</span></div></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Create workspace}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Validation (snxreg16)": {
    "prefix": "snxreg16",
    "description": "Next.js register with inline validation messages and submit gating.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:ValidatedRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  const [touched, setTouched] = useState({ name: false, email: false, password: false });",
      "  const nameError = !data.name ? \"Name is required\" : \"\";",
      "  const emailError = !data.email ? \"Email is required\" : !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(data.email) ? \"Invalid email\" : \"\";",
      "  const pwError = !data.password ? \"Password is required\" : data.password.length < 8 ? \"Min 8 characters\" : \"\";",
      "  const isValid = !nameError && !emailError && !pwError;",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setTouched({ name: true, email: true, password: true }); if (!isValid) return; await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form noValidate onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} onBlur={() => setTouched((t) => ({ ...t, name: true }))} aria-invalid={touched.name && !!nameError} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm aria-[invalid=true]:border-red-500\" />{touched.name && nameError && <p className=\"text-xs text-red-600\">{nameError}</p>}</div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} onBlur={() => setTouched((t) => ({ ...t, email: true }))} aria-invalid={touched.email && !!emailError} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm aria-[invalid=true]:border-red-500\" />{touched.email && emailError && <p className=\"text-xs text-red-600\">{emailError}</p>}</div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} onBlur={() => setTouched((t) => ({ ...t, password: true }))} aria-invalid={touched.password && !!pwError} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm aria-[invalid=true]:border-red-500\" />{touched.password && pwError && <p className=\"text-xs text-red-600\">{pwError}</p>}</div>",
      "        <button type=\"submit\" className=\"w-full rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Loading State (snxreg17)": {
    "prefix": "snxreg17",
    "description": "Next.js register with explicit loading + error state.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:LoadingRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) {",
      "    e.preventDefault(); setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) });",
      "      if (!res.ok) throw new Error(\"Could not create account.\");",
      "      router.push(\"${3:/dashboard}\");",
      "    } catch (err) { setError((err as Error).message); } finally { setLoading(false); }",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <form onSubmit={onSubmit} aria-busy={loading} className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5\">",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Create your ${4:Shelb} account</h1>",
      "        {error && <p role=\"alert\" className=\"rounded-md bg-red-50 text-red-700 px-3 py-2 text-sm\">{error}</p>}",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "        <button type=\"submit\" disabled={loading} className=\"inline-flex w-full items-center justify-center gap-2 rounded-md bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 font-medium disabled:opacity-60\">{loading && (<svg className=\"animate-spin h-4 w-4\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><circle cx=\"12\" cy=\"12\" r=\"10\" stroke=\"currentColor\" strokeWidth=\"4\" fill=\"none\" className=\"opacity-25\" /><path d=\"M4 12a8 8 0 018-8\" stroke=\"currentColor\" strokeWidth=\"4\" fill=\"none\" className=\"opacity-75\" /></svg>)}{loading ? \"Creating…\" : \"${6:Create account}\"}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Mobile First (snxreg18)": {
    "prefix": "snxreg18",
    "description": "Next.js mobile-first full-bleed register page.",
    "body": [
      "\"use client\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:MobileRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-[100svh] flex flex-col bg-white px-5 pb-8 pt-12}\">",
      "      <header className=\"text-center mb-8\"><h1 className=\"text-3xl font-bold\">${4:Shelb}</h1><p className=\"text-sm text-gray-500 mt-1\">Create your account</p></header>",
      "      <form onSubmit={onSubmit} className=\"flex-1 flex flex-col gap-4\">",
      "        <input placeholder=\"Full name\" required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-xl border border-gray-200 px-4 py-4 text-base\" />",
      "        <input type=\"email\" placeholder=\"Email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-xl border border-gray-200 px-4 py-4 text-base\" />",
      "        <input type=\"password\" placeholder=\"Password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-xl border border-gray-200 px-4 py-4 text-base\" />",
      "        <button type=\"submit\" className=\"mt-auto w-full rounded-xl bg-blue-600 hover:bg-blue-700 text-white px-4 py-4 text-base font-medium\">${6:Create account}</button>",
      "        $0",
      "      </form>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Enterprise Branded (snxreg19)": {
    "prefix": "snxreg19",
    "description": "Next.js enterprise-branded registration with SSO link and policy footer.",
    "body": [
      "\"use client\";",
      "import Link from \"next/link\";",
      "import { useState, FormEvent } from \"react\";",
      "import { useRouter } from \"next/navigation\";",
      "",
      "export default function ${1:BrandedRegisterPage}() {",
      "  const router = useRouter();",
      "  const [data, setData] = useState({ name: \"\", email: \"\", password: \"\" });",
      "  async function onSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(data) }); router.push(\"${3:/dashboard}\"); }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex flex-col bg-gradient-to-b from-slate-50 to-white px-4}\">",
      "      <div className=\"flex-1 flex items-center justify-center\">",
      "        <form onSubmit={onSubmit} className=\"w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 p-8 space-y-5\">",
      "          <div className=\"flex items-center justify-between\"><div className=\"flex items-center gap-2\"><span className=\"h-9 w-9 rounded-lg bg-blue-700 text-white grid place-items-center font-bold\">S</span><span className=\"font-semibold text-gray-900\">${4:Shelb}</span></div><Link href=\"/sso/register\" className=\"text-xs text-blue-700 hover:underline\">Use SSO</Link></div>",
      "          <h1 className=\"text-xl font-semibold text-gray-900\">Create your enterprise account</h1>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Full name</label><input required value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Work email</label><input type=\"email\" required value={data.email} onChange={(e) => setData({ ...data, email: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <div className=\"space-y-1\"><label className=\"block text-sm font-medium text-gray-700\">Password</label><input type=\"password\" minLength={8} required value={data.password} onChange={(e) => setData({ ...data, password: e.target.value })} className=\"w-full rounded-md border border-gray-300 px-3 py-2 text-sm\" /></div>",
      "          <button type=\"submit\" className=\"w-full rounded-md bg-blue-700 hover:bg-blue-800 text-white px-4 py-2 font-medium\">${6:Create account}</button>",
      "          $0",
      "        </form>",
      "      </div>",
      "      <footer className=\"py-6 text-center text-xs text-gray-400\">© ${4:Shelb} · <Link href=\"/privacy\" className=\"hover:underline\">Privacy</Link> · <Link href=\"/terms\" className=\"hover:underline\">Terms</Link></footer>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Register — Verify Email (snxreg20)": {
    "prefix": "snxreg20",
    "description": "Next.js verify-email screen shown after successful registration.",
    "body": [
      "\"use client\";",
      "import Link from \"next/link\";",
      "import { useState } from \"react\";",
      "",
      "export default function ${1:VerifyEmailPage}() {",
      "  const [resending, setResending] = useState(false);",
      "  const [sent, setSent] = useState(false);",
      "  async function resend() {",
      "    setResending(true);",
      "    await fetch(\"${2:/api/auth/resend-verification}\", { method: \"POST\" });",
      "    setSent(true); setResending(false);",
      "  }",
      "  return (",
      "    <main className=\"${5:min-h-screen flex items-center justify-center bg-gray-50 px-4}\">",
      "      <div className=\"w-full max-w-md bg-white rounded-2xl shadow-lg p-8 space-y-5 text-center\">",
      "        <div className=\"mx-auto h-12 w-12 rounded-full bg-emerald-50 text-emerald-700 grid place-items-center text-xl\">✓</div>",
      "        <h1 className=\"text-xl font-semibold text-gray-900\">Almost there!</h1>",
      "        <p className=\"text-sm text-gray-500\">We sent a verification link to your inbox. Open it to activate your ${4:Shelb} account, then continue to your <Link href=\"${3:/dashboard}\" className=\"text-blue-700 hover:underline\">dashboard</Link>.</p>",
      "        {sent && <p role=\"status\" className=\"text-xs text-emerald-700\">A fresh link is on its way.</p>}",
      "        <button type=\"button\" onClick={resend} disabled={resending} className=\"w-full rounded-md border border-gray-200 text-sm px-3 py-2 hover:bg-gray-50 disabled:opacity-60\">{resending ? \"Resending…\" : \"${6:Resend email}\"}</button>",
      "        $0",
      "      </div>",
      "    </main>",
      "  );",
      "}"
    ]
  },

  "Shelb Next Auth — useAuth Hook (snxauth01)": {
    "prefix": "snxauth01",
    "description": "Next.js client useAuth hook reading from AuthContext.",
    "body": [
      "\"use client\";",
      "import { useContext } from \"react\";",
      "import { ${1:AuthContext} } from \"${2:@/lib/auth-context}\";",
      "",
      "export function ${3:useAuth}() {",
      "  const ctx = useContext(${1:AuthContext});",
      "  if (!ctx) throw new Error(\"${3:useAuth} must be used inside <AuthProvider>\");",
      "  return ctx;",
      "}$0"
    ]
  },

  "Shelb Next Auth — AuthProvider (snxauth02)": {
    "prefix": "snxauth02",
    "description": "Next.js AuthProvider with React Context, user state and login/logout.",
    "body": [
      "\"use client\";",
      "import React, { createContext, useEffect, useState } from \"react\";",
      "",
      "export interface ${1:AuthUser} { id: string; name: string; email: string; role: string; }",
      "interface ${2:AuthContextValue} { user: ${1:AuthUser} | null; loading: boolean; login: (email: string, password: string) => Promise<void>; logout: () => Promise<void>; }",
      "",
      "export const AuthContext = createContext<${2:AuthContextValue} | null>(null);",
      "",
      "export function ${3:AuthProvider}({ children }: { children: React.ReactNode }) {",
      "  const [user, setUser] = useState<${1:AuthUser} | null>(null);",
      "  const [loading, setLoading] = useState(true);",
      "  useEffect(() => { fetch(\"${4:/api/auth/me}\").then((r) => r.ok ? r.json() : null).then(setUser).finally(() => setLoading(false)); }, []);",
      "  async function login(email: string, password: string) {",
      "    const res = await fetch(\"${5:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "    if (!res.ok) throw new Error(\"Login failed\");",
      "    setUser(await res.json());",
      "  }",
      "  async function logout() { await fetch(\"${6:/api/auth/logout}\", { method: \"POST\" }); setUser(null); }",
      "  return <AuthContext.Provider value={{ user, loading, login, logout }}>{children}</AuthContext.Provider>;",
      "}$0"
    ]
  },

  "Shelb Next Auth — useSession (snxauth03)": {
    "prefix": "snxauth03",
    "description": "Next.js client useSession hook fetching the current session.",
    "body": [
      "\"use client\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "export interface ${1:Session} { user: { id: string; email: string; role: string } | null; expiresAt?: string; }",
      "",
      "export function ${2:useSession}() {",
      "  const [session, setSession] = useState<${1:Session} | null>(null);",
      "  const [loading, setLoading] = useState(true);",
      "  useEffect(() => {",
      "    let cancelled = false;",
      "    fetch(\"${3:/api/auth/session}\", { cache: \"no-store\" })",
      "      .then((r) => (r.ok ? r.json() : null))",
      "      .then((data) => { if (!cancelled) setSession(data); })",
      "      .finally(() => { if (!cancelled) setLoading(false); });",
      "    return () => { cancelled = true; };",
      "  }, []);",
      "  return { session, loading };",
      "}$0"
    ]
  },

  "Shelb Next Auth — useUser (snxauth04)": {
    "prefix": "snxauth04",
    "description": "Next.js client useUser hook returning the current user (or null).",
    "body": [
      "\"use client\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "export interface ${1:User} { id: string; name: string; email: string; role: string; }",
      "",
      "export function ${2:useUser}() {",
      "  const [user, setUser] = useState<${1:User} | null>(null);",
      "  const [loading, setLoading] = useState(true);",
      "  useEffect(() => {",
      "    fetch(\"${3:/api/auth/me}\", { cache: \"no-store\" }).then((r) => r.ok ? r.json() : null).then(setUser).finally(() => setLoading(false));",
      "  }, []);",
      "  return { user, loading, isAuthenticated: !!user };",
      "}$0"
    ]
  },

  "Shelb Next Auth — useLogout (snxauth05)": {
    "prefix": "snxauth05",
    "description": "Next.js useLogout hook calling logout endpoint and redirecting.",
    "body": [
      "\"use client\";",
      "import { useRouter } from \"next/navigation\";",
      "import { useState } from \"react\";",
      "",
      "export function ${1:useLogout}() {",
      "  const router = useRouter();",
      "  const [loading, setLoading] = useState(false);",
      "  async function logout() {",
      "    setLoading(true);",
      "    await fetch(\"${2:/api/auth/logout}\", { method: \"POST\" });",
      "    router.push(\"${3:/login}\");",
      "    router.refresh();",
      "    setLoading(false);",
      "  }",
      "  return { logout, loading };",
      "}$0"
    ]
  },

  "Shelb Next Auth — useLogin (snxauth06)": {
    "prefix": "snxauth06",
    "description": "Next.js useLogin hook with loading and error state.",
    "body": [
      "\"use client\";",
      "import { useRouter } from \"next/navigation\";",
      "import { useState } from \"react\";",
      "",
      "export function ${1:useLogin}() {",
      "  const router = useRouter();",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function login(email: string, password: string, redirectTo = \"${3:/dashboard}\") {",
      "    setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "      if (!res.ok) throw new Error(\"Invalid credentials\");",
      "      router.push(redirectTo);",
      "      router.refresh();",
      "    } catch (e) { setError((e as Error).message); throw e; } finally { setLoading(false); }",
      "  }",
      "  return { login, loading, error };",
      "}$0"
    ]
  },

  "Shelb Next Auth — useRegister (snxauth07)": {
    "prefix": "snxauth07",
    "description": "Next.js useRegister hook with loading and error state.",
    "body": [
      "\"use client\";",
      "import { useRouter } from \"next/navigation\";",
      "import { useState } from \"react\";",
      "",
      "export interface ${1:RegisterPayload} { name: string; email: string; password: string; }",
      "",
      "export function ${2:useRegister}() {",
      "  const router = useRouter();",
      "  const [loading, setLoading] = useState(false);",
      "  const [error, setError] = useState<string | null>(null);",
      "  async function register(payload: ${1:RegisterPayload}, redirectTo = \"${4:/dashboard}\") {",
      "    setLoading(true); setError(null);",
      "    try {",
      "      const res = await fetch(\"${3:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(payload) });",
      "      if (!res.ok) throw new Error(\"Could not create account\");",
      "      router.push(redirectTo);",
      "    } catch (e) { setError((e as Error).message); throw e; } finally { setLoading(false); }",
      "  }",
      "  return { register, loading, error };",
      "}$0"
    ]
  },

  "Shelb Next Auth — useRefreshToken (snxauth08)": {
    "prefix": "snxauth08",
    "description": "Next.js useRefreshToken hook that pings refresh endpoint on interval.",
    "body": [
      "\"use client\";",
      "import { useEffect } from \"react\";",
      "",
      "export function ${1:useRefreshToken}(intervalMs = 1000 * 60 * 10) {",
      "  useEffect(() => {",
      "    let timer: ReturnType<typeof setInterval>;",
      "    async function refresh() {",
      "      try { await fetch(\"${2:/api/auth/refresh}\", { method: \"POST\", credentials: \"include\" }); } catch { /* ignore */ }",
      "    }",
      "    refresh();",
      "    timer = setInterval(refresh, intervalMs);",
      "    return () => clearInterval(timer);",
      "  }, [intervalMs]);",
      "}$0"
    ]
  },

  "Shelb Next Auth — RequireAuth (snxauth09)": {
    "prefix": "snxauth09",
    "description": "Next.js RequireAuth wrapper that redirects unauthenticated users.",
    "body": [
      "\"use client\";",
      "import { useRouter } from \"next/navigation\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "export function ${1:RequireAuth}({ children, redirectTo = \"${2:/login}\" }: { children: React.ReactNode; redirectTo?: string; }) {",
      "  const router = useRouter();",
      "  const [ready, setReady] = useState(false);",
      "  useEffect(() => {",
      "    fetch(\"${3:/api/auth/me}\", { cache: \"no-store\" }).then((r) => {",
      "      if (!r.ok) { router.replace(redirectTo); } else { setReady(true); }",
      "    });",
      "  }, [router, redirectTo]);",
      "  if (!ready) return <div className=\"min-h-screen grid place-items-center text-sm text-gray-500\">Checking session…</div>;",
      "  return <>{children}</>;",
      "}$0"
    ]
  },

  "Shelb Next Auth — RoleGuard (snxauth10)": {
    "prefix": "snxauth10",
    "description": "Next.js RoleGuard component that renders a fallback for unauthorized users.",
    "body": [
      "\"use client\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "export function ${1:RoleGuard}({ allow, children, fallback = null }: { allow: string[]; children: React.ReactNode; fallback?: React.ReactNode; }) {",
      "  const [role, setRole] = useState<string | null>(null);",
      "  useEffect(() => { fetch(\"${2:/api/auth/me}\", { cache: \"no-store\" }).then((r) => r.ok ? r.json() : null).then((u) => setRole(u?.role ?? null)); }, []);",
      "  if (role && allow.includes(role)) return <>{children}</>;",
      "  return <>{fallback ?? <div className=\"p-6 rounded-md bg-red-50 text-red-700 text-sm\">You do not have access to this section.</div>}</>;",
      "}$0"
    ]
  },

  "Shelb Next Auth — TenantGuard (snxauth11)": {
    "prefix": "snxauth11",
    "description": "Next.js TenantGuard that restricts content to a specific tenant.",
    "body": [
      "\"use client\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "export function ${1:TenantGuard}({ tenant, children }: { tenant: string; children: React.ReactNode; }) {",
      "  const [allowed, setAllowed] = useState<boolean | null>(null);",
      "  useEffect(() => { fetch(\"${2:/api/auth/me}\", { cache: \"no-store\" }).then((r) => r.ok ? r.json() : null).then((u) => setAllowed(u?.tenant === tenant)); }, [tenant]);",
      "  if (allowed === null) return <div className=\"p-6 text-sm text-gray-500\">Verifying access…</div>;",
      "  if (!allowed) return <div className=\"p-6 rounded-md bg-amber-50 text-amber-800 text-sm\">This workspace is not accessible from your account.</div>;",
      "  return <>{children}</>;",
      "}$0"
    ]
  },

  "Shelb Next Auth — ProtectedRoute (snxauth12)": {
    "prefix": "snxauth12",
    "description": "Next.js ProtectedRoute wrapping a page with optional role and loading UI.",
    "body": [
      "\"use client\";",
      "import { useRouter } from \"next/navigation\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "export function ${1:ProtectedRoute}({ children, roles, redirectTo = \"${2:/login}\" }: { children: React.ReactNode; roles?: string[]; redirectTo?: string; }) {",
      "  const router = useRouter();",
      "  const [state, setState] = useState<\"loading\" | \"allowed\" | \"denied\">(\"loading\");",
      "  useEffect(() => {",
      "    fetch(\"${3:/api/auth/me}\", { cache: \"no-store\" }).then(async (r) => {",
      "      if (!r.ok) { router.replace(redirectTo); return setState(\"denied\"); }",
      "      const u = await r.json();",
      "      if (roles && roles.length > 0 && !roles.includes(u.role)) return setState(\"denied\");",
      "      setState(\"allowed\");",
      "    });",
      "  }, [router, redirectTo, roles]);",
      "  if (state === \"loading\") return <div className=\"min-h-screen grid place-items-center text-sm text-gray-500\">Loading…</div>;",
      "  if (state === \"denied\") return <div className=\"p-6 rounded-md bg-red-50 text-red-700 text-sm\">Access denied.</div>;",
      "  return <>{children}</>;",
      "}$0"
    ]
  },

  "Shelb Next Auth — Token Store (snxauth13)": {
    "prefix": "snxauth13",
    "description": "Next.js client token store backed by localStorage with React state sync.",
    "body": [
      "\"use client\";",
      "import { useEffect, useState } from \"react\";",
      "",
      "const KEY = \"${1:shelb.token}\";",
      "",
      "export function ${2:useTokenStore}() {",
      "  const [token, setTokenState] = useState<string | null>(null);",
      "  useEffect(() => { setTokenState(typeof window !== \"undefined\" ? window.localStorage.getItem(KEY) : null); }, []);",
      "  function setToken(value: string | null) {",
      "    setTokenState(value);",
      "    if (typeof window === \"undefined\") return;",
      "    if (value) window.localStorage.setItem(KEY, value); else window.localStorage.removeItem(KEY);",
      "  }",
      "  return { token, setToken, isAuthenticated: !!token };",
      "}$0"
    ]
  },

  "Shelb Next Auth — Login Server Action (snxauth14)": {
    "prefix": "snxauth14",
    "description": "Next.js server action for login, setting an HttpOnly cookie.",
    "body": [
      "\"use server\";",
      "import { cookies } from \"next/headers\";",
      "import { redirect } from \"next/navigation\";",
      "",
      "export async function ${1:loginAction}(formData: FormData) {",
      "  const email = String(formData.get(\"email\") ?? \"\");",
      "  const password = String(formData.get(\"password\") ?? \"\");",
      "  const res = await fetch(\"${2:/api/auth/login}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ email, password }) });",
      "  if (!res.ok) throw new Error(\"Invalid credentials\");",
      "  const { token } = await res.json();",
      "  cookies().set(\"${3:shelb_session}\", token, { httpOnly: true, secure: true, sameSite: \"lax\", path: \"/\" });",
      "  redirect(\"${4:/dashboard}\");",
      "}$0"
    ]
  },

  "Shelb Next Auth — Logout Server Action (snxauth15)": {
    "prefix": "snxauth15",
    "description": "Next.js server action that clears the session cookie and redirects.",
    "body": [
      "\"use server\";",
      "import { cookies } from \"next/headers\";",
      "import { redirect } from \"next/navigation\";",
      "",
      "export async function ${1:logoutAction}() {",
      "  cookies().delete(\"${2:shelb_session}\");",
      "  await fetch(\"${3:/api/auth/logout}\", { method: \"POST\" });",
      "  redirect(\"${4:/login}\");",
      "}$0"
    ]
  },

  "Shelb Next Auth — Register Server Action (snxauth16)": {
    "prefix": "snxauth16",
    "description": "Next.js server action for registration that issues a session cookie.",
    "body": [
      "\"use server\";",
      "import { cookies } from \"next/headers\";",
      "import { redirect } from \"next/navigation\";",
      "",
      "export async function ${1:registerAction}(formData: FormData) {",
      "  const body = { name: String(formData.get(\"name\") ?? \"\"), email: String(formData.get(\"email\") ?? \"\"), password: String(formData.get(\"password\") ?? \"\") };",
      "  const res = await fetch(\"${2:/api/auth/register}\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify(body) });",
      "  if (!res.ok) throw new Error(\"Could not create account\");",
      "  const { token } = await res.json();",
      "  cookies().set(\"${3:shelb_session}\", token, { httpOnly: true, secure: true, sameSite: \"lax\", path: \"/\" });",
      "  redirect(\"${4:/dashboard}\");",
      "}$0"
    ]
  },

  "Shelb Next Auth — Refresh Server Action (snxauth17)": {
    "prefix": "snxauth17",
    "description": "Next.js server action that refreshes the auth token and updates the cookie.",
    "body": [
      "\"use server\";",
      "import { cookies } from \"next/headers\";",
      "",
      "export async function ${1:refreshTokenAction}() {",
      "  const current = cookies().get(\"${2:shelb_session}\")?.value;",
      "  const res = await fetch(\"${3:/api/auth/refresh}\", { method: \"POST\", headers: { \"Authorization\": \"Bearer \" + (current ?? \"\") } });",
      "  if (!res.ok) return { ok: false } as const;",
      "  const { token } = await res.json();",
      "  cookies().set(\"${2:shelb_session}\", token, { httpOnly: true, secure: true, sameSite: \"lax\", path: \"/\" });",
      "  return { ok: true } as const;",
      "}$0"
    ]
  },

  "Shelb Next Auth — authFetch Wrapper (snxauth18)": {
    "prefix": "snxauth18",
    "description": "Next.js authFetch helper attaching bearer token to every request.",
    "body": [
      "import { ${1:getStoredToken} } from \"${2:@/lib/token-store}\";",
      "",
      "export async function ${3:authFetch}(input: RequestInfo | URL, init: RequestInit = {}) {",
      "  const token = ${1:getStoredToken}();",
      "  const headers = new Headers(init.headers);",
      "  if (token) headers.set(\"Authorization\", \"Bearer \" + token);",
      "  if (!headers.has(\"Content-Type\") && init.body && typeof init.body === \"string\") headers.set(\"Content-Type\", \"application/json\");",
      "  const res = await fetch(input, { ...init, headers, credentials: \"include\" });",
      "  if (res.status === 401) { /* trigger re-auth flow */ }",
      "  return res;",
      "}$0"
    ]
  },

  "Shelb Next Auth — getSession Server Util (snxauth19)": {
    "prefix": "snxauth19",
    "description": "Next.js server-side getSession that reads the session cookie and resolves user.",
    "body": [
      "import { cookies } from \"next/headers\";",
      "",
      "export interface ${1:Session} { user: { id: string; email: string; role: string }; expiresAt: string; }",
      "",
      "export async function ${2:getSession}(): Promise<${1:Session} | null> {",
      "  const token = cookies().get(\"${3:shelb_session}\")?.value;",
      "  if (!token) return null;",
      "  const res = await fetch(\"${4:/api/auth/session}\", { headers: { \"Authorization\": \"Bearer \" + token }, cache: \"no-store\" });",
      "  if (!res.ok) return null;",
      "  return (await res.json()) as ${1:Session};",
      "}$0"
    ]
  },

  "Shelb Next Auth — withAuth HOC (snxauth20)": {
    "prefix": "snxauth20",
    "description": "Next.js withAuth HOC that wraps a server component and redirects on no session.",
    "body": [
      "import { redirect } from \"next/navigation\";",
      "import { ${1:getSession} } from \"${2:@/lib/get-session}\";",
      "",
      "export function ${3:withAuth}<P extends Record<string, unknown>>(Page: (props: P) => Promise<JSX.Element> | JSX.Element, options?: { roles?: string[]; redirectTo?: string }) {",
      "  return async function GuardedPage(props: P) {",
      "    const session = await ${1:getSession}();",
      "    if (!session) redirect(options?.redirectTo ?? \"${4:/login}\");",
      "    if (options?.roles && !options.roles.includes(session.user.role)) redirect(\"${5:/forbidden}\");",
      "    return Page(props);",
      "  };",
      "}$0"
    ]
  }
}
